<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements inherited by helper processes (GPU, renderer, plugin
  helpers) — electron-builder `mac.entitlementsInherit`.

  Keep this MINIMAL: helpers must NOT carry any restricted entitlements
  (ones that require a provisioning profile). AMFI kills any helper whose
  restricted entitlements aren't backed by a provisioning profile, which
  presents as an endless "GPU process exited unexpectedly: exit_code=9" loop.
-->
<plist version="1.0">
  <dict>
    <!-- V8 JIT — Electron's renderer won't start under the hardened
         runtime without these two. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.inherit</key>
    <true/>
  </dict>
</plist>
